# 아키텍처 v0.4 — 행동 에이전트 (김반장)

> 화면을 *읽고 답하던* 챗봇(v0.2/0.3)을, **사용자 대신 화면을 조작해 조회·수정·등록·삭제까지 수행하는 에이전트**로 확장한다.
> 용어는 [glossary.md](../glossary.md), 이전 단계는 [v0.3-direction.md](v0.3-direction.md).
> 이 문서는 *방향·설계 합의*이자 진행 현황 추적이다. **1단계 PoC는 구현·테스트 완료**(§15), 나머지 단계와 진행 계획은 §16 TODO. 구체 구현 시 모듈별 설계로 분화한다.

---

## 0. 한 줄 / 목표 / 무기

- **한 줄:** 에이전트는 통합(integration)하지 않는다. **사용자의 살아있는 로그인 세션에서 실제 UI를 조작**한다 — 에이전트(LangGraph)는 *계획*만, 손·눈은 익스텐션(김반장)이 *실제 화면*에서 움직인다.
- **목표:** 클로드 인 크롬 수준의 범용 브라우저 행동 + **사내 도메인 최적화**.
- **무기(클로드 인 크롬이 못 하는 것):**
  1. **사내 시스템 지식 그래프** — 범용 모델엔 *없는* 우리 레거시의 메뉴·화면·폼 지식을 그래프로 축적(§7).
  2. **도메인 정식 코드 찾기(grounding)** — "볼펜 → 정식 품목코드", 결재 규칙 등을 회사 데이터에서 찾기(§4·§5).
  3. **엔터프라이즈 거버넌스** — 마스킹·감사·허용목록(§9). 범용 제품이 안 해주는 것.
- **액션층은 직접 소유**한다(범용 루프는 상품화됨). [nanobrowser](https://github.com/nanobrowser/nanobrowser)(Apache-2.0)의 검증된 해법을 **참고**해 단단히 만든다(§10).

---

## 1. 핵심 결정 — 왜 "API"가 아니라 "실제 UI 조작"인가 (결정 기록)

세 후보를 검토하고 **UI 조작(실제 버튼 클릭)** 으로 결정했다. 근거를 남긴다.

| 방식 | 무결성 | 인증 | 레거시 무수정 | 치명적 단점 |
|---|---|---|---|---|
| **API/MCP 직접 호출** | ⚠️ 깨질 위험 | ❌ 토큰 책임 | △(기존 API면 OK) | 화면·화면전용 서버(BFF)에만 있는 **검증·파생·워크플로 건너뜀**. 저장 시 발생하는 **중간 이벤트(알림 발송·결재 라우팅·채번 등)가 누락**됨 |
| **API 콜 관찰·재현** | ⚠️ 시퀀스 누락 | △ | ✅ | 단일 콜만 재현 시 *상태 있는 시퀀스* 깨짐. 암호화 페이로드면 불가 |
| **UI 조작(버튼 클릭)** ★ | ✅ 페이지가 전부 수행 | ✅ 세션 상속·새 인증 0 | ✅ 코드 무수정 | DOM 취약·느림·대량 불리 → 채움·검증 난이도로 이동 |

**결정 이유 (핵심):** 사용자가 누르듯 **진짜 "저장"을 클릭하면, 내부에서 여러 API가 불리고 알림·결재·채번 같은 *중간 이벤트가 전부 정상 발생*** 한다. API를 직접/재현 호출하면 바로 이 이벤트들이 **누락**된다 — 이것이 API를 버린 결정타다. 또 세션을 그대로 타므로 인증·CSRF 책임이 사라진다(브라우저 안에서 실행하기 때문).

**비용은 사라지지 않고 *옮겨간다*:** "저장"의 무결성은 공짜로 얻지만, 난이도가 **"클릭 전에 폼을 *올바른 상태*로 만들기"** 로 이동한다(controlled input·종속 필드·위저드). 여기를 §3 액션층이 담당한다.

**인식은 DOM 우선, 비전은 폴백:** DOM/구조는 결정론·저비용·테스트 가능. 비전(스크린샷)은 *처음 보는·이상한 UI*도 보지만 느림·비싸므로 **막혔을 때만**. (egress가 Anthropic·Azure로 이미 승인된 경계라 비전 도입에 거버넌스 장벽은 없음 — §9)

---

## 2. 행동 분류 & 게이트 (쉬운 말)

행동을 두 종류로만 나눈다:

- **다시 하면 되는 행동** — 보기·이동·검색·입력폼 *채우기만*(저장 전). 틀려도 다시 하면 됨 → **마음껏 자동.**
- **한 번 하면 끝인 행동** — 저장·삭제. 누르면 못 되돌림 → **신중.**

매 행동 앞에 **게이트**가 "물어볼까/그냥 할까"를 결정한다:

```
gate(action) = f( 위험등급, 신뢰도, 모드 )
  위험등급  : 다시 하면 되는 / 한 번 하면 끝
  신뢰도    : 기억(그래프·메모리)에서 온 — 이 결정, 이 맥락에서 과거 성공/확인 횟수
  모드      : default | auto-fill | bypass
  → 신뢰도 ≥ 임계(위험등급·모드별) 면 실행, 아니면 HITL
```

- 이 게이트 하나가 **HITL · 메모리 기반 자율 · bypass(클로드 코드식)** 를 통일한다.
- **마찰의 진짜 원인은 "마지막 저장 1번"이 아니라 "중간의 잦은 확인들"**(이 품목 맞아? 이 코드 맞아?) → **에이전트가 그래프 DB·메모리에 경험을 쌓을수록 이 잦은 확인이 줄어든다.** "다시 하면 되는" 쪽은 공격적으로 자율화하고, **"한 번 하면 끝"인 것만 비례적·가벼운 확인**(금액 상한 내 자동, 초과 시 확인)으로 남긴다. 단, 신뢰도가 아무리 높아도 *틀렸을 때의 비용*까지 줄여주지는 못하므로, 되돌릴 수 없는 행동의 자율화는 0으로 만들지 않는다.

---

## 3. 컴포넌트 아키텍처 — 모듈과 역할·책임

```mermaid
flowchart TB
  subgraph CH["🧩 익스텐션 — 김반장 (사용자 세션, 손·눈)"]
    direction TB
    subgraph PER["인식 (Perception)"]
      DX["DataExtractor<br/>v0.2 어댑터+recipe → ScreenContext"]
      FX["FormExtractor<br/>입력 컨트롤 → FormContext"]
      SI["ScreenIdentifier<br/>지금 어느 화면인가(시그니처)"]
      VC["VisionCapture<br/>스크린샷(폴백)"]
    end
    subgraph ACT["작동 (Actuation)"]
      AE["ActionExecutor<br/>navigate·click·fill(안전주입)·select·searchSelect·waitFor·readBack"]
    end
    GOV1["익스텐션 거버넌스<br/>마스킹/deny · allow/denylist · 감사 hook"]
  end

  subgraph BR["🧠 에이전트 — AI 서비스 / LangGraph (지금은 mock 서버)"]
    direction TB
    IP["IntentParser<br/>NL → 구조화 작업(작업·대상·파라미터)"]
    PL["Planner<br/>기본 동작 시퀀스 + 그래프 경로질의 + fill-plan"]
    RS["Resolver<br/>'볼펜'→정식코드(폼 검색위젯) + HITL 후보 고르기"]
    OR["Orchestrator<br/>관찰-행동 루프 · 스텝별 진행 · LangGraph interrupt(HITL)"]
    GT["Gate/Policy<br/>위험×신뢰×모드 → 물을까/할까"]
    VR["Verifier<br/>결과 확인(성공/실패)"]
    MW["MemoryWriter<br/>정식 코드 찾기·fill-plan·성공을 그래프/메모리에 적재"]
  end

  subgraph KN["🗄️ 그래프DB/메모리 — 지식 저장"]
    SG["Site Graph<br/>화면(노드)+이동(액션경로 엣지)+폼 스키마"]
    MEM["Memory<br/>정식 코드 찾기/절차/선호"]
    MP["Mapper<br/>능동 부트스트랩 + 수동 유지(§8)"]
  end

  CH <-->|"양방향 액션 프로토콜<br/>(명령 ↓ / 관찰 ↑)"| BR
  PL <-->|경로·폼 질의| SG
  RS <--> MEM
  MW --> SG & MEM
  MP --> SG
  PER -.공급.- BR
  BR -.명령.- ACT
```

**모듈 역할·책임 (한 줄씩)** — *영역*은 그 모듈이 어디 사는지다: **익스텐션**=김반장(브라우저 속 손·눈), **에이전트**=AI 서비스/LangGraph(계획·판단), **그래프DB/메모리**=지식 저장.

| 영역 | 모듈 | 역할 | 책임 경계(하지 않는 것) |
|---|---|---|---|
| 익스텐션·인식 | **DataExtractor** | 화면 *데이터* 추출(표/카드/차트) → ScreenContext (v0.2) | 화면을 읽기만 한다. 클릭·이동은 하지 않는다. |
| 익스텐션·인식 | **FormExtractor** | *입력 컨트롤* 추출 → FormContext(필드·타입·옵션·버튼) | 어떤 입력칸이 있는지 파악만 한다. 값을 채워 넣지는 않는다. |
| 익스텐션·인식 | **ScreenIdentifier** | "지금 어느 화면" 판정(도착 감지) | 화면이 무엇인지 알아보기만 한다. 읽기용 recipe와 섞지 않는다(역할 분리). |
| 익스텐션·인식 | **VisionCapture** | 스크린샷 제공(폴백) | DOM으로 안 보일 때만 쓰는 보조 수단이다. 평소엔 쓰지 않는다. |
| 익스텐션·작동 | **ActionExecutor** | 기본 동작(primitive) 실행 — 클릭·**폼 값 입력(실제로 칸에 써넣음, 안전 주입)**·선택·검색·대기·되읽기 | 시키는 동작을 수행만 한다. *무엇을* 할지(어느 칸에 무슨 값)는 에이전트(Planner)가 정한다. |
| 익스텐션·거버넌스 | **GovernancePlane** | 마스킹·allow/denylist·감사 | 가리고·막고·기록만 한다. 업무 내용 자체를 판단하지는 않는다. |
| 에이전트 | **IntentParser** | 자연어 → 구조화 작업 | 사용자 말을 구조로 바꾸기만 한다. 실제 행동은 하지 않는다. |
| 에이전트 | **Planner** | 시퀀스·경로·*검사 가능한 fill-plan* 생성 | 계획만 세운다. 화면을 직접 누르지는 않는다. |
| 에이전트 | **Resolver** | 모호 항목 → 정식 코드 찾기(+HITL) | 모호한 말을 정식 코드로 바꾸기만 한다. 폼에 입력하지는 않는다. |
| 에이전트 | **Orchestrator** | 관찰-행동 루프·스텝 진행·HITL 멈춤/재개 | 전체 진행을 지휘한다. 클릭·입력 같은 동작을 직접 만들지는 않는다(익스텐션이 함). |
| 에이전트 | **Gate/Policy** | 물을까/할까 결정 | 물을지 그냥 할지만 정한다. 실제 행동은 하지 않는다. |
| 에이전트 | **Verifier** | 결과 검증·실패 회수 | 결과가 맞는지 확인만 한다. 다시 계획 짜는 일은 Orchestrator에 맡긴다. |
| 에이전트 | **MemoryWriter** | 성공·정식 코드 찾기·계획 적재 | 기억에 쓰기만 한다. 기억을 꺼내 쓰는 건 Planner/Resolver가 한다. |
| 그래프DB/메모리 | **Site Graph** | 화면·이동·폼 지식 보관 | 지식을 보관만 한다. 직접 행동하지는 않는다. |
| 그래프DB/메모리 | **Memory** | 정식 코드 찾기/절차/선호 누적 | — |
| 그래프DB/메모리 | **Mapper** | 그래프 생성·유지(§8) | 그래프를 만들고 갱신만 한다. 저장·삭제처럼 *바꾸는 버튼은 절대 누르지 않는다*. |

> **핵심 축(linchpin) = fill-plan.** Planner는 폼을 바로 채우지 않고 **"어느 필드에 무슨 값"의 *검사 가능한 계획*** 을 먼저 만든다. 이 하나가 **안전**(실행 전 확인)·**HITL**(계획 검토)·**테스트**(브라우저 없이 FormContext→plan 단위테스트)·**실행/계획 분리**(UI든 미래 API든 교체 가능)를 동시에 푼다.

---

## 4. 실행 순서 (등록 작업 예시) — 모듈별 역할이 드러나는 시퀀스

```mermaid
sequenceDiagram
  autonumber
  actor U as 사용자(홈)
  participant CHT as 챗(Side Panel)
  participant BR as 에이전트(LangGraph)
  participant KN as Graph DB/Memory
  participant CH as 익스텐션
  participant PG as 레거시 화면(세션)

  U->>CHT: "MRO 볼펜10·스테이플러100·A4 23개 구매신청"
  CHT->>BR: 작업 요청
  BR->>BR: IntentParser — 3개 품목으로 파싱
  BR->>KN: Planner — "구매요청 등록 폼" 경로 질의
  KN-->>BR: 액션경로(딥링크 or 메뉴 클릭 시퀀스)
  Note over BR,CH: ① 이동 (다시 하면 되는 행동 → 자동)
  BR->>CH: navigate(액션경로)
  CH->>PG: 실행(딥링크/메뉴워킹)
  CH->>BR: ScreenIdentifier — "구매요청 폼 도착" 관찰
  Note over BR,CH: ② 인식
  CH->>BR: FormExtractor — FormContext(품목 검색위젯·수량·라인그리드·저장)
  Note over BR: ③ 정식 코드 찾기 + 계획
  BR->>CH: searchSelect("볼펜")
  CH-->>BR: 후보 목록
  alt 모호
    BR->>CHT: HITL "볼펜 3종 중?"
    CHT-->>BR: 사용자 선택
  end
  BR->>KN: Resolver — 정식 코드 찾기 결과 캐시 조회/적재
  BR->>BR: Planner — fill-plan 생성
  Note over BR,CH: ④ 채움 (저장 전 = 자동)
  BR->>CH: fill/select/addRow (안전 주입)
  CH->>PG: 라인 채움·종속필드 처리
  Note over BR,CHT: ⑤ 미리보기 + 확인 (한 번 하면 끝 직전)
  BR->>CHT: 미리보기(diff/요약) — Gate: 확인 요구
  CHT-->>BR: 승인 (또는 사람이 직접 저장)
  Note over BR,CH: ⑥ 저장 — 페이지가 내부 N콜·알림·결재를 정상 수행
  BR->>CH: click(저장)
  CH->>PG: 저장 클릭
  PG-->>CH: 결과(성공/레코드ID or 검증에러)
  CH->>BR: Verifier — 결과 관찰
  alt 실패
    BR->>BR: Orchestrator — 페이지 에러 읽고 재시도/질문
  end
  BR->>KN: MemoryWriter — 성공 fill-plan·정식 코드 찾기 적재
  BR->>CHT: 완료 보고(레코드 번호)
  CHT->>U: "등록 완료 / 출처"
```

---

## 5. 모든 요청 타입 — 하나의 루프 + 공통 기본 동작

"등록 봇"이 아니다. **조회·수정·등록·삭제·승인이 *같은 루프*** 로 흐른다. 다른 건 "어떤 기본 동작(primitive)을 쓰나"와 "마지막에 *저장*이 있나"뿐.

```
공통 루프: 요청 이해 → 대상 화면 도달(이동) → 화면 인식 →
           필요 행동(읽기/채우기/바꾸기/클릭) → 필요시 확인 → 결과 보고
```

| 요청 | 핵심 기본 동작 | 한 번 하면 끝? | 도입 순서·이유 |
|---|---|---|---|
| **조회** | navigate·search·read | 없음 | **1순위** — 가장 안전·빈번, v0.2 읽기 재활용 |
| **수정** | navigate·read(기존)·fill(diff)·save | 1번 | 2순위 — 기존 레코드 기반이라 채울 게 적고 *diff 확인이 자연스러움* |
| **등록** | navigate·fill·save | 1번 | 3순위 |
| **삭제·승인** | navigate·search·click·confirm | 매우 신중 | 4순위 — 가장 높은 게이트 |

→ 별도 플로우 N개가 아니라 **기본 동작 모음 + 계획자(LLM)** 가 요청마다 조립. **"한 번 하면 끝" 게이트는 실제 저장이 있는 곳에만** 붙는다.

---

## 6. 네비게이션 — 길은 주되, 상태를 보며 간다

에이전트는 *대본*이 아니라 **상태(화면)를 보고 한 스텝씩** 정한다. 누가 이동했든(자동/사람) **"도착"만 감지**하면 이어간다.

**3단 폴백**

1. **딥링크** 있으면 → 직행.
2. **없으면(웹스퀘어 등) → 메뉴 자동 조작**: Site Graph의 *액션 경로*(대>중>소 메뉴 클릭 시퀀스)를 따라감.
3. **막히면 → 사람에게 한 단계 넘김**: "구매요청 화면으로 가주세요." → ScreenIdentifier가 도착 감지 → 이어받음. (메뉴 이동은 *사람이 제일 잘함*)

**사용자 화면에서 직접 이동(관찰 가능):** 에이전트가 *목적 화면으로 직접 이동*하고, 그 과정을 사용자가 *눈으로 본다*. 되돌릴 수 없는 단계나 모호한 지점에서는 **사용자가 관찰·확인**하고, 필요하면 사용자가 직접 한 단계 진행한다(위 3단 폴백).

> 백그라운드 탭에서 몰래 처리하는 방식은 *쓰지 않는다* — 엔터프라이즈 단일세션·서버 상태 가정과 충돌하고, 무엇보다 **사용자의 관찰·감독 기회를 없애기** 때문이다. 화면 이동이 사용자의 흐름을 끊을 수 있으니 작업 시작·완료를 챗으로 안내한다.

**모듈 분리 원칙:** 네비게이션 맵 · 화면 식별 · 액션 프로파일은 **v0.2 recipe와 별도 모듈**이다(역할 오염 금지). 저수준 "DOM 매칭 유틸"만 공유하고 *설정 아티팩트는 분리*.

---

## 7. 지식 그래프 — 우리의 무기 (모델에 없는 *사내 사전 지식(priors)*을 쌓는다)

범용 모델은 GitHub는 알아도 *사내 구매시스템*은 모른다. 그 빈자리를 **그래프로 채운다.**

```mermaid
flowchart LR
  H[("화면: 홈")] -->|"클릭: 메뉴>구매"| L[("화면: 구매 목록")]
  L -->|"클릭: 등록"| F[("폼: 구매요청 등록")]
  L -->|"클릭: 행>상세"| D[("화면: 구매요청 상세")]
  D -->|"클릭: 수정"| E[("폼: 구매요청 수정")]
  F -.스키마.- FS["FormSchema<br/>품목(검색위젯)·수량·납기·부서…<br/>필수/타입/옵션/스텝"]
  E -.스키마.- FS
```

- **노드 = 화면/상태**(목록·상세·폼). **엣지 = 이동(액션 경로)** — 딥링크가 없으니 *URL이 아니라 "클릭 시퀀스"* 로 주소를 잡는다.
- **폼 노드 = 필드 스키마**(검색위젯 여부·필수·옵션·스텝) → Planner가 fill-plan 생성에 사용.
- **네비게이션 = 그래프 경로 탐색**(현재 노드 → 목표 폼 최단 경로) → *블라인드 탐색이 아니라 결정론적*.
- **권한 변이:** 관리자 그래프 ≠ 일반 사용자 화면 → 역할별 서브그래프 고려.
- **저장소:** 바닥부터 만들지 말고 Neo4j 등 그래프DB + 메모리 레이어(Mem0/Zep) 재사용 검토.
- 이 그래프는 v0.6 **Agent Memory**의 토대이기도 하다.

---

## 8. 지속 업데이트 — 능동 부트스트랩 + 수동 유지

신규 도입 시스템을 **빨리 덮으려면** 능동이 필요하고, **틀어짐을 따라가려면** 수동이 필요하다. 둘을 합친다.

> **매핑 모드는 김반장의 한 모드다(별도 익스텐션 아님). v0.4 범위에 포함**하되, 자율 크롤이 아니라 **관리자가 핵심 흐름을 감독하며 걷는 동안 기록**하는 방식이다. 구현·교체 전략은 [§11](#11-구현-전략--포트--어댑터-목업-먼저-나중에-교체).

```mermaid
flowchart TB
  subgraph BOOT["① 능동 부트스트랩 (도입 시 1회)"]
    direction TB
    B1["메뉴·목록·*빈* 폼까지 따라가며 그래프 생성"]
    B2{"변이 버튼?<br/>(저장·삭제·승인·제출)"}
    B1 --> B2
    B2 -- "예 → denylist" --> BX["클릭 금지"]
    B2 -- "아니오(이동/조회)" --> B3["노드·엣지·폼스키마 적재"]
  end
  subgraph SAFE["가드레일"]
    G1["읽기전용 권한 계정(있으면)"]
    G2["첫 크롤 관리자 감독"]
    G3["변이 라벨 denylist"]
  end
  subgraph MAINT["② 수동 유지 (실사용 중 상시)"]
    direction TB
    M1["사용자가 화면 사용"] --> M2["라이브 화면 vs 저장 그래프 비교"]
    M2 -- "차이 발견" --> M3["자동 갱신(변경) / 자동 추가(신규)"]
    M2 -- "그래프 없음" --> M4["자동 추가"]
    M3 & M4 --> M5["drift 기록 · 필요시 재검증"]
  end
  BOOT --> MAINT
  SAFE -.보호.- BOOT
```

- **능동은 "이동만"(읽기 전용):** 메뉴·목록·*빈 폼 도달*은 안전(폼 스키마 수집 OK). **폼 제출·저장·삭제·승인·행별 액션은 절대 금지** → 변이 라벨 denylist.
- **수동 유지 = 안전한 자가성장:** 실사용 중 *라이브 vs 저장 그래프 diff* 만 자동 반영(없으면 자동 추가, 바뀌면 자동 갱신). 화면 변경·신규 추가를 *쓰면서* 따라잡는다.
- **선택적 수동 업데이트 기능**도 별도 제공(관리자가 특정 화면을 명시적으로 재학습).
- **drift 감지:** 그래프대로 갔는데 도착 못 하면 그 엣지를 의심·재학습.

---

## 9. 거버넌스 · 안전 (가로지르는 1급 평면)

- **세션 권한 상속:** 에이전트는 *사용자가 할 수 있는 것만* 한다(새 인증 0). 권한 상승 없음.
- **마스킹/deny:** 민감값은 *익스텐션을 떠나기 전* 가린다. egress(Anthropic·Azure)는 승인 경계지만 정책상 특정 필드(단가·주민번호)는 redaction.
- **Allowlist / Denylist:** 행동 허용 시스템·폼 목록 / *절대 누르면 안 되는 버튼* 목록.
- **미리보기 + 확인 + (기본) 사람이 저장:** 되돌릴 수 없는 행동은 fill-plan 미리보기 후 승인. 강한 정책은 *최종 저장만 사람이*.
- **감사 로그:** 누가·무엇을·언제·무슨 값으로. bypass의 안전망.
- **프롬프트 인젝션 가드:** 페이지 텍스트는 *데이터*지 *지시*가 아니다.
- **bypass 모드:** "다시 하면 되는" 행동엔 관대, "한 번 하면 끝"엔 금액 상한·폼별 opt-in.

---

## 10. nanobrowser에서 *참고*할 것 (소유는 우리, 지혜는 빌린다)

[nanobrowser](https://github.com/nanobrowser/nanobrowser) (Apache-2.0, 비-copyleft, TS/MV3, Planner·Navigator 멀티에이전트). 우리는 *코드를 통째로 흡수*하지 않고 **검증된 해법을 참고**해 우리 액션층을 단단히 한다:

- **MV3 서비스워커 수명** — 긴 다단계 루프 vs SW 종료를 어떻게 견디나(offscreen/keep-alive/상태 저장).
- **인터랙티브 요소 추출·인덱싱** — 클릭 대상에 안정적 핸들을 부여하는 방식.
- **액션 실행 신뢰성** — controlled input 안전 주입, 대기/재시도 패턴.
- **멀티에이전트 분리**(Planner/Navigator) — 단, 우리는 *에이전트를 서버/LangGraph로* 둔다(nanobrowser는 in-browser).
- ⚠️ 라이선스 표기 의무 준수(사본 포함·변경 표기).

---

## 11. 구현 전략 — 포트 & 어댑터 (목업 먼저, 나중에 교체)

뒷단(그래프DB·LangGraph 에이전트)이 아직 없으므로, **교체 지점을 인터페이스(포트)로 못 박고 지금은 파일/mock 어댑터로 구현**한다. 나중에 실제 백엔드가 붙을 때 *그 어댑터만* 갈아끼운다(v0.3의 "가짜 두뇌 → AI 서비스" 패턴과 동일).

| 모듈 | 지금 (v0.4) | 나중에 교체 |
|---|---|---|
| **익스텐션 채널** — 이동·인식·클릭·안전입력 | 실제 구현 | 유지 |
| **매핑 모드**(Mapper, 관리자 감독) | 실제 구현 | 유지 |
| **GraphStore**(포트) | `FileGraphStore` (JSON 파일) | → 실제 그래프DB 어댑터 |
| **Brain/Planner**(포트) | mock 서버 로직 | → LangGraph 에이전트 |

**원칙: 인터페이스는 *파일 편의*가 아니라 실제 그래프DB·LangGraph의 수요 기준으로 설계한다.** 그래야 교체가 "한 모듈 갈아끼우기"로 끝난다. 호출부(채널·Mapper)는 *파일인지 그래프DB인지 모르게* 한다.

- **GraphStore 포트**: `findPath(현재화면, 목표폼)` · `getFormSchema(screenId)` · `matchScreen(signature)` · `upsertNode/Edge` · `diff(live, stored)`
- **Brain/Planner 포트**: `parseIntent(자연어)` · `plan(intent, formContext, graph) → fill-plan/steps` · `decideGate(action) → 물을지/할지`

---

## 12. PoC 목업 — 사내 구매시스템 (`server/web/mock/`)

실제 사내 시스템 없이 테스트·시연하기 위한 화면. **결정: 구매시스템 1개부터 · DOM 친화형 · 순수 JS**(빌드 도구 없이, 프로젝트 컨벤션).

**구조 — 딥링크 없는 단일 페이지(SPA형).** 왼쪽 메뉴(대>중>소) 클릭으로 *내용만 교체* → 이동·도착 감지를 *URL이 아니라 DOM 시그니처*로 시험.
```
구매 ─ 구매요청 ─ 목록 / 등록(신규)
        └ 정산 ─ (메뉴만, 미구현)
하자 ─ (메뉴만 — 나중 2번째 시스템 자리)
```

**화면 4개**
1. **홈**(랜딩)
2. **구매요청 목록** — 표 + 행별 `[상세] [삭제]` + 상단 `[+ 신규등록]`
3. **구매요청 등록 폼** — 요청제목·부서(select)·납기일(date) + **라인 그리드**(행추가·품목 검색팝업·수량·금액 자동계산) + 합계 + 필수 검증 + `[저장]` → **확인 모달** → "요청번호 PR-2026-0042 등록됨"
4. **구매요청 상세** — 읽기 전용(조회) + `[수정]`(기존값 채운 폼)

**의도적으로 넣는 난이도 (진짜 테스트 가치)**
- **딥링크 없음** → ScreenIdentifier가 *DOM 시그니처*로 "도착"을 판정.
- **품목은 타이핑 불가, 검색팝업으로만** + "볼펜" 검색 시 **3종(흑/청/적)** → "정식 코드 찾기" + HITL 후보 고르기.
- **금액·합계는 readonly 자동계산**, 단가는 품목 선택 시 자동 채움 → 단순 `.value` 주입이 막힘 → **"안전 입력"을 진짜로 검증**.
- 필드는 *정확한 input/change 이벤트*에만 반응(controlled input 흉내).

**목업 단계 (로드맵과 정렬)**
1. 메뉴 + **등록 폼**(핵심) — 1단계 PoC
2. 목록·상세 — 조회·수정
3. 삭제·승인
4. (나중) 하자시스템 — *다른 구조*로 시스템 무관성 증명

---

## 13. 미해결 가정 · 리스크 (정직하게)

| 가정/리스크 | 영향 | 대처 |
|---|---|---|
| 에이전트 화면 이동이 사용자 흐름 방해 | 작업 중단감 | 시작·완료를 챗으로 안내, 관찰·확인 단계 제공 |
| 딥링크 존재? | 이동 비용·신뢰도 | 없으면 메뉴맵·사람 핸드오프 |
| 위젯에 안정적 handle? | 채움·클릭 신뢰도 | 없으면 비전 폴백/HITL |
| 비전 비용·비결정성 | 대량·되돌릴 수 없는 작업 | 기본 DOM, 비전은 폴백 |
| 능동 크롤 오클릭 | 실 트랜잭션 발생 | 읽기권한·denylist·감독 |
| 그래프 drift | 잘못된 이동 | 수동 diff·재검증 |

---

## 14. 단계 로드맵 (위험 낮은 데서 가치 먼저)

> 전 단계 공통 — 화면은 **구매 목업**(§12), 뒷단은 **GraphStore·Brain 포트 + 파일/mock 어댑터**(§11)로 진행. 결정론적으로 검증 가능한 로직(FormContext 추출·fill-plan·이벤트)은 `tests/`에 케이스 추가.

> 진행 현황 요약은 §16 TODO. 1~6·8단계는 목업+포트(파일/mock) 위에서 **구현·테스트 완료**, 7단계(실 백엔드)만 보류.

1. **1단계 PoC ✅(§15)** — 목업의 *메뉴 + 등록 폼*에서 액션층 최소 루프: 도착 감지 + FormContext 추출 + fill-plan + 안전 입력 + 정식 코드 찾기 + 미리보기·HITL + 저장 + 결과 검증.
2. **조회 ✅** — 목록·상세. 이동·검색·읽기.
3. **수정 ✅** — 기존 레코드 diff. "한 번 하면 끝" 게이트.
4. **등록(완성) → 삭제·승인 ✅** — 삭제 확인 게이트.
5. **매핑 모드(관리자 감독) + 지식 그래프 ✅** — Mapper가 메뉴·폼 그래프를 읽기전용 순회로 기록(`FileGraphStore`). *수동 유지(drift)는 후속.*
6. **백엔드 교체 ☐(보류)** — `FileGraphStore` → 실 그래프DB, mock Planner → LangGraph (포트 그대로, 어댑터만 교체). 외부 인프라 필요 → PoC는 목업 데이터로.
7. **메모리로 잦은 확인 제거 ✅(1단계)** — 정식 코드 찾기 누적 → HITL 감소. 성공 fill-plan 절차 재사용은 v0.6 자기개선과 합류.

> 번호와 §16의 단계 번호가 일부 다르다(§16은 구현 순서 기준). 내용은 같다.

---

## 15. 1단계 PoC — 구현 완료 (as-built)

1단계는 **부품과 핵심 루프를 자동 테스트로 증명하는 것**을 목표로 했고, 실제 크롬 익스텐션 안에서 채팅 입력부터 폼 조작까지 한 줄로 이어 시연하는 런타임 연결은 **2단계로 미뤘다**. 부품(서버 두뇌 + 익스텐션 손·눈)은 양쪽 모두 구현했으며, 자동 검증이 불가능한 브라우저 런타임 동작만 수동으로 남긴다(CLAUDE.md §6).

### 무엇을 만들었나

- **PoC 목업(구매시스템)** — [`server/web/mock/`](../../server/web/mock/). 딥링크 없는 단일 페이지로, 좌측 메뉴 클릭으로 내용만 교체한다. 요청제목·부서·납기일과 라인 그리드(행추가·품목 검색팝업·수량)를 갖추고, 금액·합계·단가는 readonly 자동계산이다. 품목은 타이핑이 막혀 있어 검색팝업으로만 고르며 "볼펜"은 흑·청·적 3종이 나온다. 무엇보다 입력칸이 **controlled input**이라 내부 모델(state)이 원천이고, 정확한 `input`/`change` 이벤트가 와야 모델이 바뀐다. 저장은 검증 후 확인 모달을 거쳐 요청번호(`PR-2026-0042`, PoC 고정)를 발급한다. `/mock/` 경로로 서빙된다.
- **익스텐션 액션층(채널)** — [`extension/content/action/`](../../extension/content/action/). v0.2 읽기(recipe)와 섞이지 않도록 별도 `UDCA` 네임스페이스에 두고 `base.js`의 저수준 유틸만 공유한다.
  - `screen-id.js` — DOM 시그니처로 "지금 어느 화면인가"를 판정(딥링크 없는 도착 감지)한다.
  - `form-extract.js` — 입력 컨트롤을 FormContext(필드·필수·옵션·검색위젯·라인그리드·저장버튼)로 추출한다.
  - `actions.js` — 안전 입력(native value setter + 이벤트 디스패치로 controlled input의 모델까지 갱신)·select·click·searchSelect·readBack·도착 대기 프리미티브.
- **서버 두뇌(mock) + 포트** — [`server/agent/`](../../server/agent/). 교체 지점을 포트(Protocol)로 못 박고 지금은 규칙/파일 어댑터로 구현했다.
  - `ports.py` — `GraphStore`(`match_screen`·`get_form_schema`·`find_path`·`upsert_node/edge`·`diff`)와 `Brain`(`parse_intent`·`plan`·`decide_gate`) 인터페이스.
  - `schemas.py` — 채널과 두뇌 사이의 규약(FormContext·Intent·**FillPlan**·GateDecision 등). FillPlan이 두 영역을 잇는 검사 가능한 축이다.
  - `mock_brain.py` — 규칙기반 두뇌. 자연어를 작업·라인으로 파싱하고, FormContext와 합쳐 fill-plan을 만들며, 저장처럼 되돌릴 수 없는 행동에는 `confirm` 게이트를 붙인다.
  - `file_graph_store.py` + `server/data/site-graph.seed.json` — 그래프 골격. 목업의 화면·이동·폼스키마 시드를 읽어 경로 질의와 폼스키마 조회를 제공한다(본격 적재는 5단계).
- **엔드포인트** — `POST /api/agent/plan`. 의도(또는 자연어)와 현재 FormContext를 받아 fill-plan을 돌려준다. FormContext가 없으면 그래프의 폼스키마로 보완한다.

### 규약의 핵심 — fill-plan이 두 영역을 가른다

서버 두뇌는 `FormContext + 의도 → fill-plan`을 만들고(파이썬으로 결정론 테스트), 익스텐션은 그 fill-plan을 받아 실제 폼에 안전 입력하고 저장한다(Playwright로 목업에서 결정론 테스트). 두 테스트가 같은 fill-plan 모양을 양쪽에서 검증하므로, 나중에 두뇌를 LangGraph로, 그래프를 실 그래프DB로 갈아끼워도 이 규약은 그대로다.

### 테스트 (`node tests/run.mjs`)

- `mock-form-extract`(browser) — FormContext 추출과 화면 식별(필드·필수·옵션·검색위젯·라인그리드·저장버튼·도착 시그니처).
- `action-primitives`(browser) — 안전 입력이 controlled input의 모델을 실제로 갱신함을 확인하고, 단순 `.value` 주입은 모델에 반영되지 않음을 **대조군**으로 보인다. 검색팝업(볼펜→3종)·자동계산·"채움 → 미리보기(게이트) → 확인 → 저장 → 결과" 전체 루프까지 검증한다.
- `server-plan`(command) — `parse_intent`·`plan`(fill-plan)·`decide_gate`·`FileGraphStore`·`/api/agent/plan`을 LLM 없이 결정론으로 검증한다.

### 2단계로 미룬 것 → **2단계에서 구현 완료(§16 참조)**

- 익스텐션 런타임 연결 — `background.js` 메시지 핸들러와 사이드패널 챗에서 "의도 입력 → 서버 계획 요청 → 익스텐션 폼 조작"을 한 줄로 잇는 작업. **구현됨**(`run-plan.js`·background 핸들러·sidepanel 브리지·챗 작업 모드). 실제 크롬 시연만 수동 확인.
- 미리보기 UI — 사이드패널에 fill-plan 미리보기 카드와 승인 버튼을 붙였다. **구현됨**.

---

## 16. TODO — 진행한 것 / 진행할 것

이 섹션은 v0.4 작업의 진행 현황을 한곳에서 추적한다. 새 단계를 시작하거나 끝낼 때 여기를 갱신한다.

### ✅ 진행한 것 (완료)

- **1단계 PoC — 액션층 최소 루프 (구현·테스트 완료, §15)**
  - 목업 구매시스템(메뉴 + 구매요청 등록폼) — `server/web/mock/`, `/mock/` 서빙. controlled input·검색팝업(볼펜→3종)·자동계산·저장 확인모달.
  - 익스텐션 액션층 — `extension/content/action/` (`screen-id`·`form-extract`·`actions`). 안전 입력·도착 감지·FormContext 추출·검색선택·되읽기.
  - 서버 두뇌(mock) + 포트 — `server/agent/` (`ports`·`schemas`·`mock_brain`·`file_graph_store`) + `POST /api/agent/plan`. fill-plan 생성·게이트 결정·그래프 골격.
  - 자동 테스트 — `mock-form-extract`·`action-primitives`(browser) + `server-plan`(command). `node tests/run.mjs` 전체 통과.
- **용어 정리** — 소프트웨어 의미의 "계약"을 **"규약"**으로 통일(업무용어 "계약금액" 등은 보존).
- **2단계 — 익스텐션 런타임 연결 (구현 완료, 수동 확인 대상)**
  - `extension/content/action/run-plan.js` — `UDCA.runPlan(plan, opts)`. fill-plan 을 순서대로 실행하고, 라인 품목은 미리 고른 코드(`resolutions`)로 해소하거나 없으면 첫 후보를 자동 선택하며, 저장은 게이트에 따라 클릭만 하거나(`commitSave=false`, 사람이 페이지에서 확인) 확인까지 누른다(`commitSave=true`).
  - `extension/background.js` — `ACTION_FILES` 주입 + `UDC_EXTRACT_FORM`(폼 인식)·`UDC_RUN_PLAN`(계획 실행) 핸들러.
  - `extension/sidepanel.js` — iframe 챗 ↔ background 브리지(`UDC_REQUEST_FORM`/`UDC_FORM`, `UDC_RUN_PLAN`/`UDC_RUN_RESULT`, origin 검증).
  - 챗 UI — `🤖 작업` 모드 + fill-plan 미리보기 카드(어느 칸에 무슨 값·필수누락·게이트) + `[채우기만]`/`[채우고 등록]`/`[취소]`. "의도 입력 → 폼 인식 → 계획 → 미리보기 → 승인 → 폼 조작 → 결과(요청번호)"를 한 줄로 연결.
  - 자동 테스트 — `action-runplan`(browser): 서버 모양의 fill-plan 을 `runPlan` 으로 실행해 채움 순서·검색선택 해소(자동/지정)·저장 게이트·실패 중단 검증.
  - *수동 확인만 남음:* 실제 크롬에 익스텐션 로드 → 목업 탭에서 Side Panel `🤖 작업` 시연(브라우저 런타임 동작은 자동 검증 불가, CLAUDE.md §6). 자동 검증 가능한 실행 로직은 위 `action-runplan` 으로 커버.
  - *알려진 단순화(후속):* 라인 품목 후보(볼펜→3종)는 현재 미리보기에서 사용자가 고르지 못하고 실행 시 첫 후보를 자동 선택한다(또는 `resolutions`/메모리로 지정). 미리보기 단계에서 후보를 사람이 고르는 본격 HITL은 다음 작업.
- **3·4·5단계 — 조회·수정·삭제 (하나의 루프, 구현 완료)** — 조회·수정·등록·삭제가 *같은 루프*(이동 → 인식 → 행동 → 필요시 확인)로 흐른다(§5). `MockBrain.plan` 이 행동 유형별로 계획을 만든다.
  - **네비게이션** — `FillPlan.nav`(메뉴/행 클릭 시퀀스)를 그래프(`find_path`)나 메뉴 직클릭으로 만들고 `runPlan` 이 먼저 이동(도착 감지)한 뒤 행동한다. 현재 화면이 폼이 아니어도 그래프 폼스키마로 계획을 세운다.
  - **조회(query)** — 목록(또는 특정 PR 상세)으로 이동 후 `readScreen` 으로 화면을 읽어 보고. 되돌릴 일 없음 → 게이트 `auto` → **확인 없이 바로 실행**(미리보기·승인 생략). 게이트 철학(§2)을 UI에 그대로 반영: `auto`는 즉시, `confirm`(등록·수정·삭제)만 미리보기+승인.
  - **수정(update)** — 목록→상세→수정으로 이동, 지정한 헤더/라인만 채우는 *diff*, 저장은 확인 게이트.
  - **삭제(delete)** — 목록으로 이동, 대상 행 삭제 클릭 → 확인 모달(되돌릴 수 없음 → confirm). 대상 PR 미지정이면 검사가능성으로 드러냄.
  - 목업 — `목록`(레코드 표·상세·삭제·신규등록)·`상세`(읽기전용)·`수정`(prefill 폼) 화면 추가, 등록 시 레코드 누적·채번.
  - 챗 UI — 작업 계획 미리보기가 행동 유형별 버튼(조회 실행 / 채우기만·채우고 등록 / 삭제 실행)과 이동 경로·조회 결과를 보여주고, 실행 뒤엔 **단계 트레이스**(이동·채움·검색선택·저장)를 체크리스트로 펼쳐 보인다(클로드 인 크롬의 "단계" 표시에 대응). 단, 우리는 *계획을 먼저 세워* 결정론적으로 실행하므로 단계가 사전에 정해져 있고 매 스텝 LLM·스크린샷이 필요 없다.
  - 자동 테스트 — `action-runplan`(browser) RP5~RP8(조회·삭제·수정·홈에서 등록), `server-plan`(command) 액션별 plan+nav.
- **6단계 — 매핑 모드 + 지식 그래프 (구현 완료)** — `🗺️ 매핑` 으로 메뉴를 **읽기전용 순회**(`UDCA.mapWalk`, 인자 메뉴 대상만 클릭)해 화면·이동·폼스키마를 모으고, `POST /api/agent/map` 이 `FileGraphStore` 에 적재(live 파일로 영속, 시드 보존). 변이 버튼(저장·삭제)은 엣지로 기록하지 않는다(denylist 가드레일, §8). 자동 테스트: `action-runplan` RP9(순회 수집) + `server-plan`(적재·denylist).
- **8단계 — 메모리(파일, 1단계) (구현 완료)** — `Memory` 포트 + `FileMemory`(JSON). 해소결과("볼펜"→정식코드)를 누적해 다음 계획에서 자동 해소 → 후보 고르기(HITL) 감소(§2). 등록·수정 성공 시 고른 코드를 `POST /api/agent/memory` 로 적재. 자동 테스트: `server-plan`(자동해소·영속).

### ☐ 진행할 것 (다음)

- **7단계 — 백엔드 교체 (§14-6, 보류)** — 현재는 PoC라 **목업 데이터(포트 뒤의 `FileGraphStore`·`MockBrain`·`FileMemory`)로 충분**하다. 실제 그래프DB·LangGraph·Mem0 연결은 외부 인프라가 필요하므로 뒤로 미룬다. 포트가 그대로라 그때 *어댑터만* 교체한다.
- **후속(품질) — 미리보기 단계 후보 고르기(HITL)** — 라인 품목 후보를 실행 전에 사용자가 고르도록(현재는 첫 후보 자동선택/메모리/`resolutions`). · **그래프 drift 재검증**(`diff` 본격화) · 메모리 확장(성공 fill-plan 절차 재사용) → v0.6 자기개선과 합류.

### 챗봇 입력 예시 — 질문(읽기) vs 지시(행동)

챗봇은 두 모드를 가진다. 평소엔 **질문(읽기)** 모드로 화면 데이터를 근거로 답하고(v0.2/0.3), `🤖 작업` 을 누르면 **지시(행동)** 모드로 폼을 대신 채운다(v0.4).

**질문(읽기) — `/demo` 같은 데이터 화면에서.** 화면에 떠 있는 데이터만 근거로 답한다(상세 예시는 [v0.3 §의 예시 질문 표](v0.3-direction.md)).
- "누적기성액이 가장 큰 현장은?" · "공종별 계약 합계가 가장 큰 공종은?" · "전체 현장 수는?"

**지시(행동) — `/mock` 구매요청 등록폼에서.** `🤖 작업` 후 자연어로 지시한다.

| 입력 예시 | 동작 | 비고 |
|---|---|---|
| `제목: 사무용품 보충, 총무팀, 2026-07-15, 볼펜10·스테이플러2 구매신청` | 헤더(제목·부서·납기)+라인까지 채워 **끝까지 등록 가능** | `채우고 등록` → 요청번호 |
| `볼펜10·스테이플러100·A4 23개 구매신청` | 라인만 채움 | 헤더 미지정 → 미리보기에 **필수 누락** 표시(사람이 폼에서 채움) |
| `포스트잇 5개 구매요청` | 라인 1건 | 〃 |
| `구매요청 목록 보여줘` | **조회** — 목록으로 이동 후 표를 읽어 보고 | 자율 — 확인 없이 바로 실행 |
| `PR-2026-0041 부서를 구매팀으로 수정` | **수정** — 상세→수정 이동 후 부서만 바꿔 저장(diff) | 확인 게이트 |
| `PR-2026-0041 삭제` | **삭제** — 목록 이동 후 그 행 삭제 | 확인 게이트(되돌릴 수 없음) |

- 현재 화면이 등록 폼이 아니어도 된다 — 에이전트가 **그래프 경로로 대상 화면까지 먼저 이동**한다(홈/목록 어디서든 지시 가능).
- **품목은 검색팝업으로만** 고른다. "볼펜"은 3종(흑/청/적)이라 실행 시 첫 후보를 자동 선택하며(또는 `resolutions`/메모리로 지정), 미리보기 카드에 `후보 고르기` 로 표시된다. 한 번 고른 품목은 메모리에 남아 다음엔 자동 해소된다.
- **헤더 추출 규칙(현재, 결정론):** 제목은 `제목:` 마커, 부서는 `…팀`(총무팀 등), 납기는 ISO 날짜(`YYYY-MM-DD`)만 인식한다. 한국어 자유형 날짜·제목 추출은 후속.

### 직접 테스트하는 법 (참고)

- 자동 전체: `node tests/run.mjs` (→ `tests/RESULTS.md`). 개별: `node tests/run.mjs action-primitives`.
- 액션 루프를 눈으로: `PWHEADED=1 PWSLOW=600 node tests/run.mjs action-primitives` (창이 떠 폼 조작을 천천히 보여줌. WSL은 WSLg 필요).
- 목업 직접 클릭: `uv run uvicorn server.main:app --reload` → http://localhost:8000/mock/ → 구매 > 구매요청 > 등록(신규).
- 두뇌만: `curl -X POST localhost:8000/api/agent/plan -H 'content-type: application/json' -d '{"intent_text":"볼펜10·스테이플러100·A4 23개 구매신청","screen_id":"pr-form"}'`.
- 파이썬 두뇌 단독: `uv run python tests/server/test_plan.py`.
- 행동 모드 전체 시연(실제 크롬, 수동): 서버 실행 후 `chrome://extensions` → 개발자 모드 → `extension/` 로드. http://localhost:8000/mock/ 에서 툴바 아이콘으로 Side Panel 열기 → `🤖 작업` 켜고 지시. 어느 화면에서든(홈 포함) 에이전트가 대상 화면으로 이동한다.
  - 등록: `제목: 비품 보충, 총무팀, 2026-07-15, 볼펜10·스테이플러2 구매신청` → `채우고 등록`.
  - 조회: `구매요청 목록 보여줘` → 표로 보고. · 수정: `PR-2026-0041 부서를 구매팀으로 수정`. · 삭제: `PR-2026-0041 삭제`.
- 매핑(관리자): Side Panel `🗺️ 매핑` → 메뉴를 읽기전용 순회해 그래프에 적재(요약 보고). 변이 버튼은 누르지 않는다.
- 메모리 확인: 같은 품목을 한 번 등록하면 다음 지시 때 미리보기에서 `후보 고르기` 표시가 사라지고 자동 해소된다(`server/data/memory.json`).
