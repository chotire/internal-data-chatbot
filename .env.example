# OpenAI API 설정 (이 파일을 .env 로 복사해서 값을 채우세요)
# cp .env.example .env
#
# ⚠️ 실제 키는 이 파일이 아니라 .env 에 넣으세요. (.env 만 gitignore 됩니다)

# 필수: OpenAI API 키
OPENAI_API_KEY=

# 사용할 모델 (코드에 하드코딩 금지, 여기서 주입)
OPENAI_MODEL=gpt-4o-mini
